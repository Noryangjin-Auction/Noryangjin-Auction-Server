
**Test Requirements:**
- Need to create `UserRepository.java`
- Must extend `JpaRepository`
- Primary operations: CRUD for `User` entity

**Files Needed:**
1. `UserRepository.java` (new interface)

**Decision:** Single file creation, using Single-File format.

---

```java
package com.noryangjin.auction.server.domain.user;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UserRepository extends JpaRepository<User, Long> {
}
```

**Implementation Notes:**
- Extends `JpaRepository<User, Long>` as required
- No custom methods yet (YAGNI principle)
- Standard CRUD operations provided by JpaRepository
