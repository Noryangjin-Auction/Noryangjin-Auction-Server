
The task requires creating a `UserRepository` interface that extends `JpaRepository`. This is a simple repository interface creation without any test context provided.

**Required Files:**
1. `UserRepository.java` - New repository interface

**Decision:**
- Single interface file
- Extends `JpaRepository<User, Long>` (assuming Long as ID type)
- No custom methods needed (just the basic CRUD from JpaRepository)

# Implementation

```java
package com.noryangjin.auction.server.domain.user;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UserRepository extends JpaRepository<User, Long> {
}
```

# Self-Check

✅ Does this satisfy the requirement? Yes - extends JpaRepository
✅ Will this compile? Yes - assuming User entity exists
✅ Is this the simplest solution? Yes - no unnecessary methods
✅ Follows package convention? Yes - `domain.user` package
