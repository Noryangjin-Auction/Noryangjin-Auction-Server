
테스트 요구사항:
- `UserRepository` 인터페이스 생성
- `JpaRepository<User, Long>` 상속
- Spring Data JPA 표준 인터페이스

필요한 파일:
1. `UserRepository.java` (새 파일)

이미 존재하는 것으로 가정:
- `User` 엔티티 (domain 패키지)

# Implementation

```java
package com.noryangjin.auction.server.domain.user;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UserRepository extends JpaRepository<User, Long> {
}
```

# Notes

- **최소 구현**: JpaRepository 상속만으로 기본 CRUD 제공
- **추가 메서드 없음**: 테스트가 요구하지 않으면 추가하지 않음
- **Spring Data JPA**: 런타임에 자동으로 구현체 생성
