
**Test Requirements:**
- No test file provided, but creating a JPA Repository
- Need: `UserRepository.java` interface
- Must extend: `JpaRepository<User, Long>`

**Decision:**
- Single interface file
- Minimal JPA repository definition
- No custom methods (YAGNI - add only when tests require)

**Output Format:** Single-File (creating one new file)

# Implementation

```java
package com.noryangjin.auction.server.domain.user;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UserRepository extends JpaRepository<User, Long> {
}
```

# Self-Check

1. ✅ Analyzed requirements (JPA repository)
2. ✅ Creating necessary file (UserRepository)
3. ✅ Format correct (Single-File)
4. ✅ Will compile (assumes User entity exists)
5. ✅ Simplest solution (no unnecessary methods)
6. ✅ Follows Spring Data JPA conventions

